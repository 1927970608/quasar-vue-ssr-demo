// import something here
import uElement from 'u-element'
import 'u-element/dist/styles/uelement.css'
// leave the export, even if you don't use it
export default ({ app, router, Vue }) => {
  // something to do
  Vue.use(uElement)
}
