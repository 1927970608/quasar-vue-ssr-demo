// import something here

// leave the export, even if you don't use it
// import store from 'src/store/index.js'
// console.log('store.product', store.product.state)
export default ({ app, router, Vue, ssrContext }) => {

  // something to do
}
